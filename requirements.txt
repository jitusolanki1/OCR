# ============================================================
# UPI OCR API — Python 3.14 Dependencies
# ============================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # Required for UploadFile

# Settings / validation
pydantic>=2.9.0                     # Python 3.14 compatible

# OCR engine (EasyOCR)
easyocr==1.7.1

# Image processing
opencv-python-headless==4.10.0.84  # Supports Python 3.14
numpy==2.1.0                       # Supports Python 3.14
Pillow>=11.1.0                     # Python 3.14 compatible (has wheels)

# PyTorch CPU-only (EasyOCR dependency)
# Install BEFORE easyocr to ensure CPU wheel is used:
#   pip install torch>=2.4.0 torchvision>=0.19.0 --index-url https://download.pytorch.org/whl/cpu
torch>=2.4.0
torchvision>=0.19.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
