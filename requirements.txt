# ============================================================
# UPI OCR API — Python 3.12 Dependencies (all packages have wheels)
# ============================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # Required for UploadFile

# Settings / validation
pydantic==2.7.1

# OCR engine (EasyOCR — supports Python 3.8-3.12)
easyocr==1.7.1

# Image processing
opencv-python-headless==4.9.0.80   # Headless = no GUI deps (Docker-safe)
numpy==1.26.4
Pillow==10.3.0                     # Used internally by EasyOCR

# PyTorch CPU-only (EasyOCR dependency)
# Install BEFORE easyocr to ensure CPU wheel is used:
#   pip install torch==2.2.2 torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchvision==0.17.2

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
