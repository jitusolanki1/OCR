# ============================================================
# UPI OCR API — Python 3.14.3+ Dependencies (all have pre-built wheels)
# ============================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # Required for UploadFile

# Settings / validation
pydantic>=2.9.0                 # Python 3.14 compatible

# OCR engine
easyocr==1.7.1

# Image processing - Python 3.14 compatible versions
opencv-python-headless>=4.10.0  # Has Python 3.14 wheels
numpy>=1.27.0                   # Python 3.14 compatible
Pillow>=11.1.0                  # Python 3.14 wheels

# PyTorch CPU-only (Python 3.14 wheels available)
# Install with: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.4.0                    # Has Python 3.14 wheels
torchvision>=0.19.0             # Has Python 3.14 wheels

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
