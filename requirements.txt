# ============================================================
# UPI OCR API — Python 3.11 Dependencies
# ============================================================

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9        # Required for UploadFile

# Settings / validation
pydantic==2.7.1

# OCR engine (EasyOCR)
easyocr==1.7.1

# Image processing
opencv-python-headless==4.9.0.80   # Headless = no GUI deps (Docker-safe)
numpy>=1.27.0                       # Python 3.14 compatible
Pillow>=11.0.0                      # Python 3.14 compatible (was 10.3.0)

# PyTorch CPU-only (EasyOCR dependency)
# Install BEFORE easyocr to ensure CPU wheel is used:
#   pip install torch>=2.3.0 torchvision>=0.18.0 --index-url https://download.pytorch.org/whl/cpu
torch>=2.3.0
torchvision>=0.18.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
